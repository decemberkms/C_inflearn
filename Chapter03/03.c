#include <stdio.h>

int main()
{

    int income, salary, bonus;

    income = salary = bonus = 100;

    printf("%d\n", income);

    // 1.0f + 2; -> float -> promotion

    // binary operator   3-2

    // unary operator   -15


    return 0;
}